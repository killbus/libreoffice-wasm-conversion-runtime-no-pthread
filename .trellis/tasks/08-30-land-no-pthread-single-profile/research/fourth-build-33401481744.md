# Fourth no-pthread build diagnosis: run 33401481744

Date: 2026-08-31

## Build result

- GitHub Actions run `33401481744`, job `99518589175`, built commit
  `f248cee83de7f521f668dce293ecaba78071fcfb`.
- The run was triggered once with `clean_build=true`.
- Patch application and configuration succeeded.
- Compilation stopped in `toolkit/source/awt/vclxtoolkit.cxx`; no native
  artifact was produced and the runtime gates were correctly skipped.

## Failure

Clang reported:

```text
error: no member named 'LibreOfficeKit' in namespace 'comphelper'
```

The new constructor guard referenced
`comphelper::LibreOfficeKit::isActive()`. The pinned LibreOffice tree declares
that API in `include/comphelper/lok.hxx`, but its older
`toolkit/source/awt/vclxtoolkit.cxx` did not include that header. The current
upstream file contains both the include and the guard; the initial backport
copied only the guard.

## Correction prepared for the fifth build

The no-pthread patch now ports the complete upstream change by adding
`#include <comphelper/lok.hxx>` and retaining the
`!comphelper::LibreOfficeKit::isActive()` constructor guard. This is a
compile-time dependency correction only; the third-build runtime diagnosis and
chosen synchronization behavior are unchanged.

## Local validation before the fifth build

- All five patches apply in order to LibreOffice commit
  `d1c9e0e4e1ddeb24fe8f93e56860b3765043f8b1` with `--fuzz=0`, including the
  newly materialized `configmgr`, `salhelper`, and `toolkit` sparse-checkout
  paths.
- The patched `vclxtoolkit.cxx` contains both `<comphelper/lok.hxx>` and the
  `!comphelper::LibreOfficeKit::isActive()` constructor guard.
- Focused no-pthread, source, release, and retry tests pass: 93 tests.
- The non-native matrix passes: 320 tests passed and 67 native-artifact tests
  skipped.
- The packaging simulation passes all seven no-pthread contract cases.
- TypeScript type checking, all JavaScript bundles, shell syntax, and
  `git diff --check` pass.
- ESLint reports zero errors and 23 pre-existing warnings.

The reviewed correction is ready for one fifth clean native build.
